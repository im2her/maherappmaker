# CORE_SERVICE_TOKEN: Required for the Maher Core engine.
# This token is injected automatically at runtime.
CORE_SERVICE_TOKEN="YOUR_CORE_TOKEN"

# APP_URL: The URL where this platform is hosted.
# This is injected automatically at runtime.
APP_URL="YOUR_APP_URL"
